import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.seevarlog.switchpuzzle",
  appName: "스위치 퍼즐",
  webDir: "dist",
  android: {
    backgroundColor: "#14161c",
  },
};

export default config;
